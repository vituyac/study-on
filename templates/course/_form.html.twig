{{ form_start(form) }}
    {{ form_row(form.code) }}
    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    <button class="btn btn-primary mb-2">{{ button_label|default('Сохранить') }}</button>
{{ form_end(form) }}
